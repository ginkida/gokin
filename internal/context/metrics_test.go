package context

// Metrics-specific tests live in predictor_test.go alongside the predictor
// tests (both target the context package). This file exists only to keep the
// package's test file list discoverable; if you split metrics into its own
// suite, move the TestContextMetrics_* functions here.
